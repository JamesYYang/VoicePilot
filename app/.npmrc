# Electron 的二进制不走 npm registry，而是由 @electron/get 从 GitHub Releases 单独下载。
# 国内网络直连 GitHub 会 fetch failed（表现为 npm install 成功但 electron 起不来）。
# 这里统一指向 npmmirror，避免每次重装或 CI 都要手动设环境变量。
#
# 验证方式：删掉 node_modules/electron 重装，或 npx electron --version

electron_mirror=https://npmmirror.com/mirrors/electron/

# electron-builder 打包时还要下载 winCodeSign / nsis / nsis-resources 等资源，
# 同样走镜像，否则 dist:win 会卡在下载。
electron-builder-binaries_mirror=https://npmmirror.com/mirrors/electron-builder-binaries/
