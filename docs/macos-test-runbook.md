# macOS 一次性复验手册（总入口）

> **本次要验的代码**：`main` 上到 `2590057` 为止的一批改动（2026-09-12 ~ 09-15）：悬浮条内闭环、采纳写回（Plan 2B）、常用语（F16）、悬浮条窗口行为修复，以及 **macOS 采纳写回的两个真机问题修复**（`2590057`）。
> **怎么用这份文件**：按「阶段 0 → 6」顺序走，每项有步骤、预期、要记什么；跑完在第八节按格式回报。
> **细节不在这里**（避免三份文档漂移）：失败时的踩坑细节、失败提示与 `reason` 的对照表、已接受的代价，都在专题手册里：
> - `docs/adopt-injection-test-runbook.md`（采纳写回）
> - `docs/common-phrases-test-runbook.md`（常用语 F16 + 第十四/十五节的两批真机修复）
> - 本文件附录 A/B/C = 原来的 M1 采集诊断 / 端到端 / 打包版踩坑（历史，仍然有效）
>
> **唯一需要记住的判定原则**：自动化全绿 ≠ 功能可用。凡涉及**焦点、窗口激活、全局快捷键、权限、鼠标命中的**结论，只有真机能验；而且**焦点类结论必须用打包 `.app` 再跑一遍**（开发模式从终端起的是 `Electron.app`，那类问题根本看不出来 —— 附录 C.2 的 NSPanel 坑就是这么来的）。
>
> **✅ 已跑完两轮，阶段 0–6 全部通过。**
> - **2026-09-15（控制台启动）**：除采纳写回的两个问题（已在 `2590057` 修掉，见阶段 2）外，其余用例通过。
> - **2026-09-16（打包 `VoicePilot.app`，从 Finder 启动 —— 以此轮为准）**：按 `docs/release-checklist.md` 的清单走完，**阶段 0–6 全过、无一项触发处置，也没有需要特别记录的观察项**。阶段 0 五项数字与预期完全一致（`tsc` 干净、SM 77/77、INJECT 24/24、UI 138/138、BAR 17/17）；**5.2 的打包版 `.node` 能 `dlopen`，未加 `afterPack` adhoc 签名**（降级触发点未触发）；焦点类四条（2.3 / 3.1 / 3.2 / 采纳后光标留在目标）在打包版复跑通过；6.1 打包版不弹 Key 表单。
> - 本手册自此保留为**回归清单**：改 macOS 焦点 / 窗口 / 注入 / 全局快捷键相关代码后，仍按阶段 0 → 6 重跑（阶段 2 与阶段 5 每次都不能省）。

---

## 〇、上 Mac 之前（必做，否则测的不是这份代码）

**`2590057` 那批修复已 push**（当前 `origin/main` = `2996dc7`，含 `2590057` / `40d1655` / `2996dc7`）。到 Mac 上先同步并核对版本：

```bash
cd voicepilot
git pull
git log --oneline -1      # 期望 2590057（或更新）；不是的话，你测的是旧代码
git status                # 期望干净
```

> ⚠️ **`git pull` 拿不到未 push 的提交**。若 `git log -1` 比期望的旧，说明这批没带过去 —— 让 Windows 侧先 push（或直接把仓库带过去），否则你测的是**修之前**的代码。

前置（一次性，见附录 A 的详细说明）：

```bash
cd app && npm install
cd .. && cp .env.example .env    # 填 DASHSCOPE_API_KEY / DASHSCOPE_WORKSPACE_ID
```

### 权限（都要在「系统设置 → 隐私与安全性」里给）

| 权限 | 用在哪 | 何时弹 |
|---|---|---|
| 麦克风 | 采集音频 | 首次运行弹，必须点「允许」 |
| **辅助功能** | 全局快捷键；`CGEventPost` 发 Cmd+V；`AXIsProcessTrusted` 检查 | **不会弹**，要手动勾上 —— 开发模式勾 **终端**（或 `Electron`），打包版勾 **VoicePilot** |

---

## 一、阶段 0：环境与自动化门禁（先跑，不用手测）

```bash
cd voicepilot/app

# 1) 类型检查
npx tsc --noEmit                                   # 期望：无输出

# 2) 五个主进程自测（期望全部退出码 0）
VP_SM_SELFTEST=1 npx electron .                    # 期望「共 77 项，通过 77，失败 0」
VP_STORE_SELFTEST=1 npx electron .                 # 期望「[自测] 通过 …」全 true
VP_SHORTCUT_SELFTEST=1 npx electron .              # 同上
VP_I18N_SELFTEST=1 npx electron .                  # 同上
VP_INJECT_SELFTEST=1 npx electron .                # 期望「共 24 项，通过 24」；**这行是「koffi 能在 macOS 加载并调用」的实证**

# 3) 渲染自测（必须先 build，否则跑的是 dist 里的旧产物）
npm run build && VP_UI_SELFTEST=1 npx electron .   # 期望「共 138 项，通过 138」

# 4) 悬浮条几何自测（真窗口 + 真屏幕，需要显示器；**Mac 首验**）
VP_BAR_SELFTEST=1 npx electron .                   # 期望「共 17 项，通过 17」
```

- 4) 会**真的闪现悬浮条**并短暂抢一次焦点（选择器态会主动 focus），别当成故障。
- 4) 里任何一项失败都值得整个带回：它量的是贴边不漂移、选择器高度、不可见时必须穿透 —— 这三条在 macOS 上**首跑前从未执行过**（2026-09-15 / 16 两轮均已跑过且全过，此处保留为回归提醒）。
- 记：每条命令的最后一行汇总 + `echo $?`。
- **2026-09-16 实测**：五项数字与预期**完全一致**（`tsc` 无输出、SM 77/77、INJECT 24/24、UI 138/138、BAR 17/17）。

---

## 二、阶段 1：听写主链路冒烟（不必重跑 M1 采集）

> M1 采集诊断与端到端**已于 2026-09-08/09 在 Mac 上通过**（内置麦 + 蓝牙双轮，「双时钟不同源」的头号嫌疑已排除）。**换了机器 / 换了耳机 / 怀疑采集**时才重跑 → 走附录 A。
> 下面这五条是这两天的改动**新引入**的，必须过一遍。

```bash
cd app && npm start          # 开发模式
```

| # | 操作 | 预期 |
|---|---|---|
| 1.1 | 光标放 TextEdit → 按 `⌥Space` → 说一句（含中英数，如「明天下午三点，和 Acme 团队过一下 Q3 预算」）→ 等停顿 → 再按一次停止 | 草稿灰字、停顿后**每个定稿句单独一行**转白字（09-12 改的分段判据）；停止后进 `reviewing` 可编辑 |
| 1.2 | 点「润色」 | 结果出现在**条下半区**（不是弹窗）；模型为 `deepseek-v4-flash-0731` |
| 1.3 | 观察条高变化 | 文本变多时条**向上长高**（上限 620），停止/关闭后**收回到基础高度** |
| 1.4 | 设置页「快捷键」块：录制一个新组合（例如 `⌥J`） | 提示成功且**立刻生效**（不重启）：旧键失效、新键可用；录一个已被别的软件占用的组合 → 提示「该快捷键已被占用，请换一个」，且**旧键仍然有效**；录制时按 `Esc` → 取消且不提交；按纯修饰键 → 提示「这个按键不能用作快捷键」且**留在录制态** |
| 1.5 | 采纳（写回） | 见阶段 2 —— 这是本次最重的一项 |

---

## 三、阶段 2：采纳写回（Plan 2B）—— ✅ 已验（含打包版）

> macOS 那套实现（`libobjc` 取/置前台、`AXIsProcessTrusted`、`activateWithOptions:` / `yieldActivationToApplication:`、`CGEventPost` / `CGEventPostToPid` 发 Cmd+V）**2026-09-15 已在 Mac 上跑过一轮**，当时暴露两个问题，都已在 `2590057` 修掉：
> - **① 点「采纳」回填不了**：旧实现只给 V 贴 Command 标志再丢进 HID tap —— 修饰键常还没生效，事件还可能贴回悬浮条自己；而成功判据只看前台 pid，于是表现成「没报错也没写回」。现改为整串 `[⌘↓ V↓ V↑ ⌘↑]`，并优先 `CGEventPostToPid` 投到目标 pid。
> - **② 回填成功、条也关了，目标应用却拿不到焦点**：现改为「先关条 → 等编辑区拆完（两帧）→ 再还键盘」，并且 macOS 空闲态窗口直接 `hide()`。成因与修法见 PRD §5.6 的 2026-09-15 更正。
>
> ✅ **2026-09-16 已在打包版（从 Finder 启动）复验：两个问题都不再复现，2.1–2.6 全过。** 修后版本此前未在任何 Mac 环境上跑过这一缺口已关闭。
>
> 失败提示与 `reason` 的对照表、终端的「误报成功」为什么不算 bug：`docs/adopt-injection-test-runbook.md` §二。

**触发**：`⌥Space` 说一句 → `reviewing` → 点「采纳」。

| # | 用例 | 步骤 | 预期 |
|---|---|---|---|
| 2.1 | **未授权 → 授权 → 成功**（macOS 独有） | ①「辅助功能」里**取消勾选** VoicePilot / Electron → ② TextEdit 里点好光标 → 说一句 → 点「采纳」 → ③ 去授权（悬浮条 →「打开应用」→ 设置 → 权限 →「打开系统设置」）→ ④ **不重启**，回到 TextEdit 再点一次「采纳」 | ②悬浮条**不关闭**、提示「未获得辅助功能权限，无法自动写回。已复制到剪贴板，请手动粘贴」、剪贴板里已有文本；③深链能跳到辅助功能页（跳不到就记下来，这是要记的坑）；④文本落进 TextEdit、条关闭。**特别记**：授权后是否需要重启/重触发才生效（`AXIsProcessTrusted()` 理论上实时，没实测过） |
| 2.2 | **目标应用矩阵** | 每个应用重复：点进输入位置 → `⌥Space` → 说中英数混排的一句 → 停止 → 点「采纳」 | 逐格记三件事：①文本**是否真落进那个输入位置** ②条**是否关闭** ③失败时提示原文。应用：TextEdit / 浏览器（地址栏 + 网页 textarea 各一次）/ Terminal 或 iTerm / Word 或 Excel（没装记「未测」）。**终端上允许「条关了但文本没进去」**——判据是「窗口到前台」不是「粘贴被消费」，照实记成「误报成功」，不要当缺陷 |
| 2.3 | **A2 回归 + 采纳后光标留在目标**（问题 ② 的正身） | TextEdit 里**持续打字**，中途按 `⌥Space` 说一句再停，全程不点悬浮条；点「采纳」后**不碰鼠标**，接着打字 | `warming`/`listening`/`draining` 三态**焦点不跳**、打字继续落进 TextEdit；采纳后文本追加到同一输入框，且**关条之后不点一下就能继续打字**（这一条就是 2026-09-15 的缺陷 ②：以前必须先在目标里点一下）。**这条打包版必须再跑一遍**（阶段 5）—— **2026-09-16 已在打包版通过** |
| 2.4 | 剪贴板**从不还原** | 先往剪贴板放哨兵串 `SENTINEL-12345` → 听写一句 → 采纳 | 成功后剪贴板 = **识别/润色文本**，不是哨兵（哨兵被覆盖且不恢复）；失败时剪贴板里**仍是那段文本**，可直接手动粘贴 |
| 2.5 | 目标窗口最小化 / 已关闭 | ①光标在 TextEdit → **最小化** → 在别的窗口按快捷键 → 采纳 ②点好光标 → 触发 → **采纳前关掉 TextEdit** → 点采纳 | ①**macOS 没有反最小化实现**，如实记录实际行为（失败但提示可感知也算过，只要条不关、剪贴板留文本）②预期提示「目标窗口已关闭，已复制到剪贴板，请手动粘贴」。**Mac 特别观察项**：若只关了**窗口**而应用进程还在，按实现推断**不会**判 `stale`（会置前成功但文本无处可落）—— 这条纯属推断，实测并记录 |
| 2.6 | **写回期间用户抢走了状态**（`2590057` 一并修掉的既有竞态） | 点「采纳」后**立刻连按两次快捷键**（趁写回那 100+ ms 还没结束） | **下一次听写不能被吃掉**：第二次按键照常开始一次听写（进 `warming`/`listening`，能正常录到结果）。旧实现在采纳收尾时会按「当前状态」分派，在 `idle` 上开一段新会话、在 `warming` 上把它取消掉 —— 表现是「连按两下什么都没录到」。此条不需要条再被关一次（它已经在为新会话服务） |

---

## 四、阶段 3：常用语 F16 —— ✅ 已验（含打包版）

> 用例细节与失败判据：`docs/common-phrases-test-runbook.md`（用例 1–7 + 第十四/十五节）。
> **前置**：先存两条常用语 —— 说一句 → `reviewing` → 点条头**书签图标** → 条底提示「已存为常用语」。

**快捷键**：主键 `⌥Space`；**常用语键 `⌥Shift+Space`**（设置页可改）。

| # | 用例 | 步骤 | 预期 |
|---|---|---|---|
| 3.1 | **选择器能拿到键盘（最高风险）** | 光标放 TextEdit → 按**常用语键** | 弹出选择器（条内），**搜索框进态即获焦**：直接敲字就进框、不需要先点一下；`↑`/`↓` 移动高亮**且底层应用不跟着滚动**；输入过滤后 `Enter` 选中 → 条切 `reviewing`、正文就是那一条。**若不成立**（敲字没反应 / 要先点一下 / 方向键带动底层应用）→ 说明「macOS 面板能收键盘」的假设被证伪，**停下来回报，不要在实现里绕** |
| 3.2 | 关闭与**焦点归还** | ①`Esc` 关闭 ②再按一次常用语键关闭 ③打开后先点一下浏览器，再按常用语键（**别用 Esc**，它此刻落在浏览器上） | ①选择器消失、**TextEdit 重新是前台**、光标还在原处、能直接继续打字 ②同 ①（这条平台行为待验，要单独确认） ③选择器关闭且**焦点不被拽回** TextEdit（归还前的闸门生效） |
| 3.3 | 存 → 选 → 写回 | 光标放 TextEdit → 常用语键 → 挑中那条 → 点「采纳」 | 文本写回（走阶段 2 同一条通道）；回 Studio「常用语」页确认那条 `used_at` 刷新（下次排最前） |
| 3.4 | 常用语**不进历史** | 记下 Studio 历史页当前条目数 → 用常用语走一遍「选中 → 采纳」 → 再正常听写一次 | 用常用语后条目数**不变**；正常听写 **+1** |
| 3.5 | 第二个快捷键的冲突与录制 | 设置页「常用语快捷键」块：①改成与主键相同 ②改成被别的软件占用的组合 ③录制时按 `Esc` ④录制时按纯修饰键 ⑤改成功后立刻按新键 | ①②提示「该快捷键已被占用，请换一个」且**主键/原短语键都不失效**；③退出录制且不提交；④提示「这个按键不能用作快捷键，请换一个」且**留在录制态**；⑤不需要重启即可打开选择器 |
| 3.6 | 空态与边界 | ①删光常用语 → 按常用语键 ②存一条**首行 > 40 字**的长文本 → 打开选择器 → 选中 ③Studio 手写一条，标题留空 | ①显示「还没有常用语。在结果里点书签图标存一条。」（不是「无匹配」那条文案）②**标题**截断成 40 字符 + `…`；选中后**编辑区里是完整正文**（截断只在展示层；正文被截 = 数据层缺陷，停下来报）③标题显示「未命名」 |

**顺手把这两批真机修复也复验了**（同一次跑完，不额外花时间）：

| # | 项 | 怎么做 | 预期 |
|---|---|---|---|
| 3.7 | 选择器**每次**都要有合身高度 | 连做 10 轮「按常用语键打开 → `Esc` 关掉」 | 每轮都能看到**全部条目**（不再只看到第一条） |
| 3.8 | 一次性提示不跟进选择器 | 存一条常用语（条底出「已存为常用语」）→ 关条 → 再打开选择器 | 条底**不再**出现那句提示 |
| 3.9 | **不可见的条不得吃掉屏幕右下角的点击** | ①冷启动后**不碰**悬浮条，直接去 Chrome 点网页右下角按钮 ②唤起一次条、鼠标移到条上、关掉，再点那个按钮 ③若①就失败：从托盘**退出 VoicePilot** 再点同一个按钮 | ①②都可点、鼠标变手型；③**退出后变好**=确实是我们挡的（回报）；**不变好**=另有成因，别归因到我们。顺手记失效区域是否约等于右下角 560×148 |

---

## 五、阶段 4：悬浮条窗口行为 —— ✅ 已验（含打包版）

| # | 项 | 怎么做 | 预期 |
|---|---|---|---|
| 4.1 | 贴边**不漂移** | 连做十几轮「听写 / 开关选择器」（跨若干分钟） | 条始终贴在**工作区**右下角（macOS 的工作区已排除菜单栏与 Dock），不会一格一格往下沉进 Dock；关闭后仍贴右下角 |
| 4.2 | 条本身可交互 | 鼠标移到条上 → 点「采纳 / 复制 / 润色 / 书签 / 打开应用」；移开后再点条底下的应用 | 按钮可点；移开后**底下的应用恢复可点**（穿透回来） |
| 4.3 | 不占 Dock / 不抢场 | 听写全程看 Dock | 条的窗口**不在 Dock 里露图标**；关闭条不退出应用（托盘还在） |
| 4.4 | 长文本后再触发 | 说一段长文本把条顶到较高，然后关闭、再触发一次短的 | 窗口**收回到基础高度**，不会带着上一段的高窗 |

> ⚠️ **Dock 自动隐藏**是另一条成因：那种设置下 macOS 会把工作区算成整屏，条离屏幕底 24px，Dock 弹出时会盖住条底。**看到请记下来**（连同「Dock 位置：底部/左侧/自动隐藏」），不要当成 4.1 的失败。

> ⚠️ **改过条的窗口参数就要回头重跑 2.3 与 3.2**（2026-09-15 新增的约束）：macOS 上空闲态窗口是**直接隐藏**的，而采纳收尾里「等编辑区拆完」那一步靠的是**隐藏窗口还能出帧**。改动 `main.js` `createBar` 的参数（尤其 `backgroundThrottling`）或挪动 `hide()` 的时机，都可能让帧饿死 —— 那时会走 1500ms 超时后路，焦点归还变成「**晚一点**」而不是不发生。标志是控制台出现一条 `[采纳] 拆条两帧在 …ms 内没到齐` 的告警；看到它说明帧链路已经退化了，别只当噪声。

---

## 六、阶段 5：打包 `.app` 复验（**必须单独跑**）

理由：开发模式 `.node` 从 `node_modules` 加载、**绕过 asar**；开发模式前台本来就是 `Electron.app`，**焦点问题看不出来**。

```bash
cd voicepilot/app
npm run dist:mac          # 会先跑 scripts/prepack-check.mjs
```

- **prepack-check 会拦**：缺 `app/electron/endpoint.built.json`（或里面有占位/非 https）就打包失败。只为复验打包行为时可以绕过：`npx electron-builder --mac`；**要发给人试用**则必须填真值：
  ```bash
  cp app/electron/endpoint.example.json app/electron/endpoint.built.json
  # endpoint 字段要填**完整 URL**（含 /config），写成 https://host 会每台机器 404
  ```
- 产物：`app/release/mac*/VoicePilot.app`（外加 dmg）。**焦点类用例一律从 Finder 启动**，不要从终端起。

> ✅ **本阶段已于 2026-09-16 完成**（打包 `VoicePilot.app`，从 Finder 启动，按 `docs/release-checklist.md` 逐项走）：**5.1–5.5 全过**，其中 **5.2 的打包版 `.node` 能 `dlopen`、未加 `afterPack` adhoc 签名** → **降级触发点未触发**，Plan 2B 不做纯剪贴板降级。6.1 见第七节。
>
> 保留 2026-09-15 那轮的教训：那一轮是在控制台里启动的（属「从终端起」），**不能替代本阶段** —— 虽然它确实暴露了焦点类缺陷（采纳后目标应用拿不到焦点），但按上面 2026-09-11 的教训（附录 C.2），**从 Finder 启动时激活路径不同，两种启动方式的结论不能互相顶替**。这正是本阶段必须单独跑的理由；改 macOS 焦点 / 窗口 / 注入代码后仍要重跑。

| # | 检查 | 预期 |
|---|---|---|
| 5.1 | koffi 的 `.node` 在 asar 外 | `VoicePilot.app/Contents/Resources/app.asar.unpacked/node_modules/@koromix/koffi-darwin-*/<子目录>/koffi.node` 存在（子目录名以实际产物为准）—— **2026-09-16 通过** |
| 5.2 | **未签名/adhoc 的 `.node` 能否 `dlopen`（头号问题）** | 应用能起来且**不报 koffi 加载错误**；打包版里把 2.2 的 TextEdit 那一格跑通一次。若启动即报 koffi 加载失败 → 修法是 `afterPack` 里对 `.node` 做 adhoc 签名（`codesign -s -`）。**这一步过不去就触发降级决策**（退回纯剪贴板方案），先回头改设计再动实现，别硬上 —— **2026-09-16 通过：未加签名即可 `dlopen`，降级未触发** |
| 5.3 | 焦点类用例在打包版复跑 | **2.3（A2 回归）、3.1（选择器抢键盘）、3.2（焦点归还）、2.2 里的采纳后光标是否留在目标** —— 这四条在打包版各再跑一次 —— **2026-09-16 四条全过** |
| 5.4 | 凭据 | 打包版**没有 `.env`**。开发模式存的 Key 在打包版**解不出来**（另一套钥匙串条目）→ 要么内网端点可用（最省事），要么在「设置 API Key」表单里重填一次 —— **2026-09-16 通过：内网端点可用，未弹表单** |
| 5.5 | 权限对象 | 打包版勾的是 **VoicePilot**（不是终端/Electron）；2.1 的未授权路径在打包版可再确认一次（这也是 F12 权限引导的复验）—— **2026-09-16 通过** |

---

## 七、阶段 6：顺手清掉（可选，但一直挂着）

| # | 项 | 怎么做 | 预期 |
|---|---|---|---|
| 6.1 | **M5-A：打包版从内网端点取 Key**（**2026-09-16 已在打包版 Mac 侧验过**：不弹「设置 API Key」表单，装完即用） | 打包版首次启动（走公司内网 / 代理） | 设计预期：**不弹**「设置 API Key」表单，即「装完即用」。排查时可从终端起二进制看日志里的 `[授权] 凭据来源=` 是 `env` / `cache` / `endpoint`。若报 `UNABLE_TO_VERIFY_LEAF_SIGNATURE` → 先查公司根证书是否被信任，**不要**改成 `rejectUnauthorized:false`（见附录 C.3） |
| 6.2 | 描述性文案 | 顺手看一眼麦克风权限弹窗文案 | 期望出现「VoicePilot 需要使用麦克风进行语音识别」（来自 `app/package.json` 的 `NSMicrophoneUsageDescription`） |

---

## 八、结果怎么回报

每条按这个格式（**只说发生了什么，不要写「应该」**）：

1. **环境**：Mac 型号 + macOS 版本（如 MacBook Air M2 / macOS 15.x）；**是否打包版**；用的是哪个麦克风（内置 / 蓝牙型号）；Dock 位置与是否自动隐藏。
2. **预期 vs 实际**：阶段的编号 + 实际现象。
3. **失败时的提示原文**：照抄（它直接对应内部 `reason`，对照表见采纳写回手册 §二）。
4. 涉及输入效果的，附一句**含中英数**的实测文本。
5. 阶段 0 的每条命令最后一行汇总 + 退出码。

**跑完回填三处状态表**（这是本项目「未验」标记的规矩）：

- `docs/adopt-injection-test-runbook.md` §一（采纳写回的状态表）
- `docs/common-phrases-test-runbook.md` §一（F16 状态表 + 第十四/十五节的复验清单）
- 本文件 §〇/阶段表 —— **全部打勾前，在汇报里一律写「未验」，不得声称 Plan 2B / F16 完成。**

> **2026-09-15 与 2026-09-16 两轮的结论都已回填进上面三处，其中 09-16 那轮是打包版从 Finder 启动。** 就本手册的 macOS 阶段 0–6 而言，**Plan 2B 与 F16 已全部打勾**：F16 的选择器键盘输入 / 焦点归还、Plan 2B 的采纳写回（含 `2590057` 修后的两个问题与 2.6 的竞态），在开发模式与打包版各过了一遍。
>
> ⚠️ **但这两个功能都还不能声称「完成」**，缺口在 macOS 之外，且不属于本手册：
> - `docs/adopt-injection-test-runbook.md` 用例 3 —— **Windows 管理员权限窗口**（验的是「失败可感知」），从头到尾没验过；
> - M5-A 自身未收口：**F13 诊断导出**还没做（试用期收不到日志，同事反馈只能靠条上提示原文 + 截图）。
>
> 📌 **环境信息缺一行**：本文件要求每轮记「Mac 型号 + macOS 版本 + 麦克风 + Dock 位置与是否自动隐藏」，两轮都没记进来。上面这些结论都不依赖它，但下次跑（或对方追问机型相关问题时）请顺手补一行 —— 尤其 Dock 自动隐藏那条例外（阶段 4 的 ⚠️），只有记了才知道有没有踩到。

---

# 附录 A：M1 采集诊断（原「测试一」）

> 背景：PRD §5.2 要求 Windows 与 macOS 各出一份实测数据。领导 2026-09-05 在 MacBook 上体验明显劣于 Windows（「只能识别到几秒前说的话」），疑因两个机制叠加：①老 demo 的整数抽取 bug（已修）②**图的水槽接 `ctx.destination` 时，图由输出设备时钟拉动，与采集设备不同源**（Windows 实测慢 0.45%，换软件水槽后残余 −0.005%）。
> **2026-09-08/09 已在 Mac 上通过（内置麦 + 蓝牙各一轮），头号嫌疑排除**；本节保留为「换机器 / 换耳机 / 怀疑采集」时重跑的完整流程。

```bash
cd app
VP_OPEN_DIAG=180000 npm start
```

**它会自动做**：打开诊断窗口 → 采集 3 分钟 → 停止 → 导出 WAV → 把结果 JSON 打到终端。**全程不用说话**，静音跑即可。

跑完终端里会有一行以 `[渲染] [M1] {...}` 开头的 JSON，这就是结果；也可以点诊断窗口右下角「复制结果 JSON」。

### 判定标准（同一把尺子）

| 指标 | 合格线 | 不合格意味着 |
|---|---|---|
| 输出字节率 | 32000 B/s，偏差 ≤ **±2%** | 重采样比例错了，ASR 听不到正常语速 |
| 图时钟速率 | 1.000000 ± **0.001** | 图由输出设备时钟拉动（双时钟不同源），越说越滞后 —— **macOS 头号嫌疑** |
| 掉帧（音频空洞） | 必须 = **0** | 渲染线程没赶上，音频有空洞，ASR 漏字 |
| 批处理滞后变化 | ≤ **±200ms** | 主线程追不上实时，滞后持续增长 |

> 「掉帧 = 0」但「图时钟速率偏」= 音频被整体拉伸千分之几，人耳听不出、ASR 不敏感，**无害可过**；「掉帧 > 0」才是缺陷。这两行要一起看。

**回报**：直接贴那行 `[M1] {...}` JSON，并附 Mac 型号 + macOS 版本 + 用的哪个麦克风 + 有没有报错。

---

# 附录 B：端到端（原「测试二」）

```bash
cd app && npm start
```

**前置**：`.env` 已配好 + 麦克风权限 + 辅助功能权限。
**操作**：按 `⌥Space` 说话，看悬浮条是否实时出字（灰字草稿 → 停顿定稿转白字）。
**重点**：**内置麦克风和蓝牙耳机各测一轮**并对比（领导的 MacBook 疑因蓝牙耳机输入/输出时钟不同源）。

> 这两天的改动把这条链路也动过（每句一行、润色模型、条高自适应）—— 但那些已并入阶段 1 的冒烟项，这里只在**换设备**时重跑。

---

# 附录 C：打包版真机踩坑（2026-09-11 补记）

打包 `VoicePilot.app` 并真机跑通后，补记三个**只在打包版出现、开发模式（`npm start`）不复现**的问题。改 macOS 相关代码前先看这里，别重复踩。

### C.1 打包后托盘图标看不到

macOS 的 `Tray` **不认 `.ico`**，拿到空图就等于没有托盘。现已在 `app/electron/main.js` 的 `loadTrayImage()` 里按平台分流：macOS 用 `build/voicepilot-icon-56.png` 缩成 18pt / 36pt（@1x/@2x）两个表示。

> 遗留：用的是彩色 logo，未设 template 图，深色菜单栏下对比度可能不足。

### C.2 唤出悬浮条后前台应用丢焦点，关掉也找不回

**这不是回归，是打包才暴露。** 开发模式从终端起的是 `Electron.app`、前台本来就是它，所以看不出问题；打包成 `VoicePilot.app` 从 Finder 启动后，快捷键唤出悬浮条会把本应用激活，前台输入框随即丢焦点。

修法（结论见 PRD §5.6 的 2026-09-11 更正）：悬浮条 `type:'panel'` + `focusable:false`，并用 `app.setActivationPolicy` 在 `'accessory'`（空闲）与 `'regular'`（有可聚焦窗口时）之间切换。

> 阶段 5 的 5.3 就是在防这一类回归。

### C.3 公司内网走代理时证书校验失败

报错形如 `UNABLE_TO_VERIFY_LEAF_SIGNATURE`。原因是**内网网关用公司 CA 重签了阿里云证书**，而 Node 的 `ws` / `fetch` 默认只认 Mozilla 捆绑 CA，不读系统信任库。

修法：`app/electron/tls-ca.js` 把系统已信任的 CA 并入默认列表（**保留证书校验，不要改成 `rejectUnauthorized:false`**）；`session.js` 建连时显式传 `ca`；`app/package.json` 的 `LSEnvironment` 里另有 `NODE_USE_SYSTEM_CA=1` 兜底。

**换机器仍报证书错时**：先确认公司根证书已装进系统钥匙串并被信任 —— 在 Electron 里 `tls.getCACertificates('system')` 非空即说明读到了（2026-09-11 本机实测：默认 118 个，并入系统后 200 个）。

> 顺带一提：打包版没有 `.env`，首次启动会弹「设置 API Key」窗。开发模式存下的 Key 在打包版**解不出来**（走的是另一套钥匙串条目），需要重填一次。
